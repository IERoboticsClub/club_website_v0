{
  "name": "backend",
  "version": "1.0.0",
  "description": "At first id do a ME stack mongo express.js stack for simplicity, then with time it could be nice to migrate to bun.js and SQL",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "nodemon": "^3.0.1"
  }
}
